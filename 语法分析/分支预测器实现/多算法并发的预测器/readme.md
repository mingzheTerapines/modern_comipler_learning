to compile:g++ *.cpp -o *.out
to run:./*.out (*=predictorname)
to test different itrace.out, change the " ifstream fin("itrace3.out");" to  "ifstream fin("itrace2.out") =;" or " ifstream fin("itrace.out");"
